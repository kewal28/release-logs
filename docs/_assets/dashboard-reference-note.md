# Dashboard design reference

The product dashboard targets a modern “business dashboard” layout: left sidebar, gradient stat cards, optional chart area, activity panel, and a sortable changelog table.

Copy your reference PNG into this folder as `dashboard-reference.png` if you want it versioned with the repo (optional).
